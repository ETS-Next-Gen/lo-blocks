# MainPane Block

## Overview

MainPane is a structural marker used inside SideBarPanel to designate the main content area. It is not rendered directly.

## Details

Created using `createStubBlock()` in `componentMap.js`. See **SideBarPanel** for usage and documentation.
