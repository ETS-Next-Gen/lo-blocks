# Distractor

Marks an incorrect answer option inside `ChoiceInput`. See `ChoiceInput` for full documentation.

## Usage

```xml
<ChoiceInput>
  <Key>Correct answer</Key>
  <Distractor>Wrong answer A</Distractor>
  <Distractor>Wrong answer B</Distractor>
</ChoiceInput>
```

Graded by `KeyGrader`.
